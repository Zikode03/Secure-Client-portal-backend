# API Modules

This folder is the target home for module-oriented API code.

Use one subfolder per business module and keep controllers, endpoint models, and API-specific policies close to that module.
